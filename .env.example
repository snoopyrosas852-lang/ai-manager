# 后端 API 基础地址（留空则使用 Mock）
# 会话审计真实数据：填 http://localhost:3101（需先启动 server/）
VITE_API_BASE_URL=http://localhost:3101

# 钉钉企业 CorpId（钉钉免登鉴权用）
VITE_DINGTALK_CORP_ID=dingxxxxxxxx

# 强制使用 Mock 数据，不请求后端（开发调试用，可选）
# VITE_USE_MOCK=true
